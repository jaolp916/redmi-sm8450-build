diff --git a/arch/arm64/boot/dts/qcom/sm8450-diting-storage.dtsi b/arch/arm64/boot/dts/qcom/sm8450-diting-storage.dtsi
index d2f1c78..e7b3a90 100644
--- a/arch/arm64/boot/dts/qcom/sm8450-diting-storage.dtsi
+++ b/arch/arm64/boot/dts/qcom/sm8450-diting-storage.dtsi
@@ -62,7 +62,7 @@
        partitions {
                compatible = "fixed-partitions";
                #address-cells = <1>;
                #size-cells = <1>;
 
-               sys {
+               sys {  /* 解锁sys分区为可读写 */
                        label = "sys";
                        reg = <0x1F000000 0x6000000>;  /* 分区起始地址和大小 */
-                       read-only;  /* 原始配置：只读 */
+                       /* 移除read-only属性，改为可读写 */
                };
        };
